{
  "name": "permissor",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "lowdb": "^1.0.0",
    "mongoose": "^5.5.12",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-router": "^5.0.0",
    "react-router-dom": "^5.0.0",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "mongo-mac": "mkdir -p mongo-db; mongod --dbpath mongo-db --port 27088",
    "start-mongo-mac": "npm run mongo-mac > 1  | npm run start"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
